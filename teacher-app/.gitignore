# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Local env overrides (the committed .env holds public EXPO_PUBLIC_ values)
.env*.local

# Logs / misc
npm-debug.*
yarn-debug.*
yarn-error.*
.DS_Store
*.tsbuildinfo

# Environment switcher files (contain keys — never commit)
.env.dev
.env.prod
